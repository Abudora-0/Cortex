{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "buildCommand": "node scripts/prepare-db.mjs && prisma generate && prisma db push --skip-generate --accept-data-loss && next build",
  "framework": "nextjs"
}
